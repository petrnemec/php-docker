FROM php:7.0-fpm

RUN apt-get update \
 && apt-get -y install git mysql-client unzip \
 && docker-php-ext-install bcmath mbstring pdo_mysql pdo_pgsql \
 && curl -sS https://getcomposer.org/installer \
    | php -- --install-dir=/usr/local/bin --filename=composer